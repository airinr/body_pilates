import 'package:flutter/material.dart';

class AppColors {
  // Ganti ke Very Light Blue (Background)
  static const backgroundPink = Color(0xFFEFF6FF);

  // Ganti ke Primary Blue (Royal Blue / Blue 500)
  static const primaryPink = Color(0xFF2196F3);

  // Ganti ke Dark Blue (Blue 700/800)
  static const darkPink = Color(0xFF1565C0);

  // Ganti ke Soft Blue (Pastel Blue / Blue 100)
  static const softPink = Color(0xFFBBDEFB);

  static const textGrey = Colors.grey;
  static const white = Colors.white;
}
